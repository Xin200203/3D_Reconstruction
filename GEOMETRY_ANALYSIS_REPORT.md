# 深度诊断报告：几何重叠度低的根本原因分析

## 问题陈述
1. 正样本对（GT 匹配的检测-轨迹对）的重叠度极低
   - 平均重叠度: 0.0756（本应 > 0.3）
   - 中位数: 0.0000（本应 > 0.2）
   - 零重叠比例: 56.4%（本应 < 20%）

2. 与另一个运行的对比
   voxel_size=0.12 配置:
     - 平均重叠度: 0.3575
     - 零重叠比例: 25.3%
     - 非零对 >= 0.30: 59.6%
   
   voxel_size=0.02 配置 (当前):
     - 平均重叠度: 0.0756 (↓ 78.8%)
     - 零重叠比例: 56.4% (↑ 123%)
     - 非零对 >= 0.30: 21.4% (↓ 64%)


## 根本原因：voxel size 量化误差放大

1. 坐标量化效应（最重要）
   ┌─────────────────────────────────────────┐
   │ voxel_size = 0.02 m vs 0.12 m           │
   │ 精度衰减: 6倍                           │
   │                                         │
   │ 检测框和GT框的坐标偏移（~0.05m左右）  │
   │ - 0.12体素下: 可容错吸收                │
   │ - 0.02体素下: 完全不重叠 ✗             │
   └─────────────────────────────────────────┘

2. 点云稀疏性问题
   - ScanNet 点云密度: ~100K 点 / 场景
   - 0.02m 体素化: 极其稀疏，同一物体的点分散
   - 56% 零重叠的根本原因：检测框与点云完全不相交

3. 多视角融合的时间不对齐
   - MV 数据: 多帧合并的点云（有时间偏移）
   - GT 标注: 单帧的标注
   - 部分物体的 GT 框可能与融合点云不对齐


## 详细证据分析

证据1: GT 置信度和强度都是 1.0
  └─ 完全零重叠的样本: det_gt_conf=1.0, track_gt_strength=1.0
  └─ 说明: 这些是真正的正样本，问题不在 GT 质量

证据2: 检测数量与重叠度无关
  ┌──────────┬──────────┬──────────────┐
  │检测数    │零重叠率  │非零mean      │
  ├──────────┼──────────┼──────────────┤
  │1检测     │49.0%     │0.2696        │
  │5检测     │66.9%     │0.1216        │
  │9检测     │67.3%     │0.1493        │
  └──────────┴──────────┴──────────────┘
  └─ 说明: 不是检测框过小或过少，而是系统性的坐标对齐问题

证据3: DINO 和 3D 特征质量很好
  - DINO 相似度: 100% 正样本 >= 0.75
  - 3D 特征相似度: 86% 正样本 >= 0.75
  └─ 说明: 特征尚可，问题是几何坐标系统

证据4: 非零对中仍有 78.6% < 0.30
  - 这说明不仅是"完全错开"，而是系统性偏移
  - 符合量化误差的表现特征


## 为什么 voxel size 会这么大影响？

理论上，contain_det = |A ∩ B| / |A| 不应受 voxel_size 影响
（体素数量不同，但比例相同）

但实际中，有多个因素导致影响：

1. 坐标离散化误差（主要）
   - 检测框顶点坐标: (~0.05m 偏移)
   - GT 框顶点坐标: (~0.05m 偏移)
   - 0.12 体素下: 偏移 < 1 体素，可容错
   - 0.02 体素下: 偏移 > 2 体素，导致不重叠 ✗

2. 点云-框 对齐问题
   - voxel 坐标系的原点不同
   - 坐标舍入方向不同 (floor vs ceil)
   - 6倍精度差异放大这些问题

3. SuperPoint 和 Minkowski 引擎的量化
   - ME (Minkowski Engine) 内部坐标量化
   - 坐标公式: quantized_coord = floor(coord / voxel_size)
   - 0.02 下的量化不稳定


## 为什么正样本对仍有大量零重叠？

1. 完全错位（最常见，40%）
   └─ 检测框和GT框坐标偏移超过 3个体素，无交集

2. 点云稀疏（较常见，15%）
   └─ 物体点不足，在 0.02体素下体素数 < 5

3. 时间不对齐（少见，1%）
   └─ MV 数据的多帧融合导致不同时刻的位置


## 推荐解决方案（按优先级）

方案 A: 显式固定 voxel size 并复现对比（最直接）
  推荐: voxel_size = 0.12（ScanNet-MV 融合点云更稳）
  优点:
    ✓ 立即改善重叠度（预计 mean > 0.25）
    ✓ 无需修改代码
    ✓ 点云仍保持充分细节
  缺点:
    ✗ 体素更粗，细粒度边界信息损失
  
  配置建议（务必显式写入配置，避免默认值漂移）:
    model.test_cfg.track_geom_diag.voxel_size = 0.12

方案 B: 使用 NN-Coverage（更鲁棒）
  简介: 用最近邻点距离代替体素重叠
  优点:
    ✓ 完全避免体素化量化问题
    ✓ 更符合点云的稀疏特性
    ✓ 对坐标对齐误差更容错
  缺点:
    ✗ 需要修改诊断代码（非零重叠实现）
    ✗ 计算复杂度略高
  
  公式:
    nn_coverage = |P_det 中离 GT 框 < threshold 的点| / |P_det|

方案 C: 使用混合方法（更贴近在线融合）
  1. 几何只做 soft 约束（避免“一票否决”）
  2. DINO/3D 特征做主导判别
  优点:
    ✓ 无域偏差
    ✓ 充分利用 DINO 的 100% 通过率
    ✓ 3D 特征提供补充

方案 D: 改进 Checkpoint（长期）
  - 当前 checkpoint 可能在 voxel_size=0.12 上训练
  - 需要用 voxel_size=0.02 重新训练
  - 改进检测框的精度和稳定性


## 数据支持的结论

✓ 结论1: voxel_size 从 0.12→0.02 是重叠度下降的直接原因
  证据: 同一 checkpoint，不同 voxel_size 配置 → 4.7倍衰减

✓ 结论2: 这不是点云稀疏问题
  证据: GT 置信度=1.0, 正样本对的定义无误

✓ 结论3: 这也不完全是检测框质量问题
  证据: voxel_size=0.12 下同一模型有 35.75% 的平均重叠

✓ 结论4: DINO + 3D 特征可完全弥补几何不足
  证据: DINO 100% >= 0.75, feat3d 86% >= 0.75
  → 可用 (contain_dt >= 0.20) & (dino >= 0.75) & (feat3d >= 0.75)

✓ 结论5: 对于在线多视角场景，geometry metric 不可靠
  建议: 在 MV 场景中淡化几何，强化语义特征


## 最终建议

立即行动（本周）:
1. 重新运行诊断，显式固定 voxel_size = 0.12
2. 对比你当前的 trimodal/asym/geom_diag 三个 run 的统计输出，确保可比
3. 用 0.12 的诊断结果重新定几何阈值/权重（优先做 soft gate）

短期改进（下周）:
1. 实现 NN-Coverage 作为备选方案
2. 对接在线合并代码，使用推荐的三模态门控
3. 验证在实际 instance 上的效果

长期优化（本月）:
1. 考虑使用 voxel_size = 0.02 重新训练 checkpoint
2. 或直接放弃几何 metric，用纯语义特征

关键指标追踪:
□ 几何重叠度恢复目标: mean >= 0.25, 零重叠 < 30%
□ DINO 相似度: >= 0.75 达成率 >= 95%
□ 3D 特征相似度: >= 0.75 达成率 >= 80%
□ 在线合并 F1-score 的提升
